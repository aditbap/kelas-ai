import { DashboardPlaceholder } from '@/components/dashboard-placeholder';

export default function InstructorDashboardPage() {
  return (
    <DashboardPlaceholder
      title="Content & grading dashboard"
      description="Authoring modules, publishing to cohorts, and grading submissions will live here."
      builtIn="it's built out in BUILD_PLAN.md Phases 7 and 9 (content authoring, assignments & grading)"
    />
  );
}
